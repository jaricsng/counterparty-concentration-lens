# Dev toolchain for the Counterparty Concentration Lens.
# Install:  pip install -r requirements-dev.txt
#
# These are the local quality + security gates that mirror CI and pre-commit.
# Runtime/module dependencies live in each module's own requirements.txt.
#
# Pins use lower bounds (>=) for a prototype so you pick up patch fixes.
# To FULLY lock for reproducibility, generate a hashed lockfile from this:
#   pip install pip-tools
#   pip-compile --generate-hashes -o requirements-dev.lock requirements-dev.txt
# (or use `uv pip compile`), then install from the .lock in CI.

# --- Orchestration of hooks ---
pre-commit>=4.0

# --- Lint + format (ruff replaces flake8 + black + isort) ---
ruff>=0.8

# --- Formatter (black, per CLAUDE.md; CI runs `black --check`) ---
#     PINNED EXACTLY to the .pre-commit-config.yaml black rev so the local hook
#     and CI never disagree on formatting. Bump both together (or run
#     `pre-commit autoupdate` and update this pin to match).
black==26.5.1

# --- Type checking ---
mypy>=1.13

# --- Tests ---
pytest>=8.3
pytest-cov>=6.0
hypothesis>=6.0          # property-based tests (metric invariants)

# --- Security ---
bandit[toml]>=1.8        # SAST; reads config from pyproject.toml
pip-audit>=2.7           # dependency vulnerability scan
