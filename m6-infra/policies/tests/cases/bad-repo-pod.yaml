apiVersion: v1
kind: Pod
metadata:
  name: bad-repo
  namespace: lens-demo
  labels:
    app.kubernetes.io/part-of: counterparty-concentration-lens
spec:
  containers:
    - name: app
      image: evil.example/malware:latest
      securityContext:
        privileged: false
      resources:
        limits:
          cpu: "500m"
          memory: "512Mi"
