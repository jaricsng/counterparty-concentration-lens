apiVersion: v1
kind: Pod
metadata:
  name: no-label
  namespace: lens-demo
spec:
  containers:
    - name: app
      image: lens/app:0.1.0
      securityContext:
        privileged: false
      resources:
        limits:
          cpu: "500m"
          memory: "512Mi"
