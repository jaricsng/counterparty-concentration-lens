# M0 runtime dependencies. Pinned for reproducibility (prototype).
# Dev/quality tooling (ruff, black, mypy, pytest, ...) lives in the repo-root
# requirements-dev.txt.
rdflib==7.6.0
requests==2.34.2
