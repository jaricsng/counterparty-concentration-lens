# Python
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/

# dbt
target/
dbt_packages/
logs/

# Data / DB artifacts (synthetic, regenerable)
*.db
*.duckdb
*.sqlite

# Triplestore local data
fuseki-data/

# Secrets / env
.env
*.key

# OS / editor
.DS_Store
.idea/
.vscode/

# Large vendored FIBO ontology blob — regenerable via vendor/fibo/fetch_fibo.sh.
# We commit the fetch script + checksum + attribution README, not the ~8 MB file
# (it exceeds the 2 MB pre-commit large-file gate). See vendor/fibo/README.md.
vendor/fibo/*.ttl
fibo-reasoner-venv/

# Apache Jena Fuseki server distribution (downloaded locally for the demo)
m0-ontology/.fuseki/
apache-jena-fuseki-*/
# Fuseki runtime working dir (shiro.ini, system store) created on first start
m0-ontology/run/
